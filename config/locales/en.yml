en:
  errors:
    entity_is_blank: "%{entity} with this id is not present"
    mutation:
      only_for_customers: Only customers can run this mutation
      only_for_managers: Only managers can run this mutation
    query:
      only_for_customers: Only customers can run this mutation
      only_for_managers: Only managers can run this mutation
    orders:
      shoes_is_out_of_stock: Currently these shoes are out of stock
